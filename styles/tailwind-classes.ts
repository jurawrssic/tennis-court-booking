const globalClasses = {
  container:
    'container grid max-h-[calc(100vh-8rem)] w-5/6 mx-16 bg-white rounded-lg bg-white',
};

const selectComponentClasses = {
  selectArea: 'select-area flex flex-col',
};

const daypickerComponentClasses = {};

const availableTimeslotComponentClasses = {
  availableTimeSlots: 'available-time-slots grid auto-rows-min',
};

const dialogComponentClasses = {};

export {
  globalClasses,
  selectComponentClasses,
  availableTimeslotComponentClasses,
};
